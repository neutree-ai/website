---
// Hero section - First impression with clear value proposition
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="hero-left">
        <div class="hero-header">
          <h1 class="hero-brand">Neutree</h1>
          <h2 class="hero-title">
            Enterprise-grade Private<br />Model-as-a-Service Platform
          </h2>
        </div>
      </div>
      <div class="hero-right">
        <p class="hero-text">
          Deploy and scale AI models in your own infrastructure with
          production-ready features and hardware flexibility.
        </p>
        <div class="hero-cta">
          <a
            href="https://github.com/neutree-ai/neutree"
            target="_blank"
            rel="noopener noreferrer"
            class="btn btn-primary"
          >
            Get Started
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: var(--spacing-hero-top) var(--padding-horizontal-desktop) var(--spacing-5xl);
  }

  .container {
    max-width: var(--max-width-content);
    margin: 0 auto;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 14px;
    align-items: flex-end;
  }

  .hero-left {
    display: flex;
    flex-direction: column;
  }

  .hero-header {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
  }

  .hero-right {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-2xl);
    padding: 12px 0;
  }

  .hero-brand {
    font-family: var(--font-sans);
    font-size: var(--font-size-hero-title);
    font-weight: 600;
    line-height: var(--line-height-normal);
    letter-spacing: var(--letter-spacing-tight);
    color: var(--color-text-primary);
    margin: 0;
  }

  .hero-title {
    font-family: var(--font-sans);
    font-size: var(--font-size-hero-subtitle);
    font-weight: 400;
    line-height: var(--line-height-normal);
    letter-spacing: var(--letter-spacing-tight);
    color: var(--color-text-primary);
    margin: 0;
  }

  .hero-text {
    font-family: var(--font-mono);
    font-size: var(--font-size-hero-desc);
    font-weight: 400;
    line-height: var(--line-height-normal);
    letter-spacing: var(--letter-spacing-tight);
    color: var(--color-text-primary);
    margin: 0;
  }

  .hero-cta {
    display: flex;
    gap: var(--spacing-xs);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-xs);
    padding: 8px 12px;
    border-radius: var(--radius-md);
    font-family: var(--font-sans);
    font-weight: 500;
    font-size: var(--font-size-button);
    line-height: var(--line-height-tight);
    transition: all 0.2s ease;
    cursor: pointer;
    border: none;
    text-decoration: none;
  }

  .btn-primary {
    background-color: var(--color-text-primary);
    color: var(--color-bg);
    box-shadow: var(--shadow-button-primary);
  }

  .btn-primary:hover {
    background-color: rgba(12, 40, 73, 0.9);
    transform: translateY(-2px);
    box-shadow: var(--shadow-button-primary);
  }

  .btn-outline {
    background-color: var(--color-bg);
    color: var(--color-text-primary);
    border: 1px solid var(--color-border-strong);
    box-shadow: var(--shadow-button-outlined);
  }

  .btn-outline:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-button-outlined);
  }

  /* Tablet Responsive */
  @media (max-width: 1279px) {
    .hero {
      padding: var(--spacing-hero-top) var(--padding-horizontal-tablet) var(--spacing-4xl);
    }

    .hero-content {
      grid-template-columns: 1fr;
      gap: var(--spacing-2xl);
    }
  }

  /* Mobile Responsive */
  @media (max-width: 767px) {
    .hero {
      padding: var(--spacing-hero-top) var(--padding-horizontal-mobile) var(--spacing-3xl);
    }

    .hero-content {
      grid-template-columns: 1fr;
      gap: var(--spacing-xl);
    }

    .hero-right {
      gap: var(--spacing-xl);
    }

    .hero-cta {
      flex-direction: column;
    }

    .btn {
      width: 100%;
    }
  }
</style>
